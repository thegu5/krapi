export enum Region {
	// Unknown = 0,
	// Oceania = 1,
	Asia = 2,
	Europe = 3,
	NorthAmerica = 4,
	// SouthAmerica = 5
}

export enum MatchResult {
	Draw = -1,
	Loss = 0,
	Win = 1,
}

export enum LeaderboardOrder {
	Ascending = 0,
	Descending = 1,
}
